opencv-python>=4.9
numpy>=1.26

# mediapipe 1.0.x aborts on macOS with
#   graph_service.h: Check failed: service_ Service is unavailable
# inside DrishtiMetalHelper, on both the CPU and GPU delegate. Stay on 0.10.x.
mediapipe>=0.10.30,<1.0

# macOS: native pointer back end (pixel-precise scrolling, real double clicks).
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"

# Everywhere else: fallback pointer back end.
pyautogui>=0.9.54; sys_platform != "darwin"
